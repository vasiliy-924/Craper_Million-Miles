# API base URL (used by the browser for client-side requests)
# For local dev with Docker: http://localhost:8000
# For local dev without Docker (API on same machine): http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
